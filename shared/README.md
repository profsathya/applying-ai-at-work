# Shared Assets

This folder is for assets used across both courses — rubrics, glossary references, capability descriptions, templates, and other shared materials — once content development begins.

Currently contains the sprint HTML template in `templates/`.
